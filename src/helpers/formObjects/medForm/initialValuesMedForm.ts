interface Props {
    completeName: string;
    age: number;
    fototype: number;
    fps?: number;
};

export const initialValuesMedForm: Props = {
    completeName: '',
    age: 0,
    fototype: 0,
    fps: 0,
};